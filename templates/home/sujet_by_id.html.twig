{% extends 'base.html.twig' %}

{% block title %}Hello{% endblock %}

{% block body %}
    <div><a href="/index">Accueil</a> </div>
    <div style="text-align: center"><h1>Titre du Sujet</h1></div>
<div style="font-size: 1.5rem; text-align: center">
    {{ sujet[0].title }}
</div>
    <div><h2>Tous les messages du sujet  {{ sujet[0].title }}</h2></div>
    <div>
        <div>
            <ul>
                {% for message in messages %}
                    <li>
                        Message : {{ message.text }} <button><a href="{{path('app_home_message_modify', { id: message.id })}}">Modifier</a></button></br>
                        Date de création {{ message.createdAt|date("d/m/Y") }}
                    </li>
                {% endfor %}
            </ul>
        </div>
    </div>
{% endblock %}